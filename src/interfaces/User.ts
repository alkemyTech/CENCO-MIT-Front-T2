export interface User {
    id: string
    rut: string
    name: string
    surname: string
    email: string
    password: string
    country: string
    phone: number
    role: string
    deletedDate: Date
}